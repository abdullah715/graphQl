```
models
    <model Name>
        index.js -> Mongoose Schema
        mutation -> all mutation fields for the model export as fields {}
        queries -> all queries fields for the
        model export as fields {}

```
